<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="Sudoku.Pages.SudokuPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:converters="clr-namespace:Sudoku.UI.Converters"
    NavigationPage.HasNavigationBar="False">
    <ContentPage.Resources>
        <converters:NumbersButtonConverter x:Key="NumbersButtonConverter" />
    </ContentPage.Resources>

    <ContentPage.Content>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="3*" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid BackgroundColor="Red" />

            <Grid Grid.Row="1">
                <CollectionView
                    x:Name="collectionView"
                    ItemsSource="{Binding Numbers}"
                    HorizontalOptions="Center"
                    Margin="20">
                    <CollectionView.ItemsLayout>
                        <GridItemsLayout
                            Orientation="Vertical"
                            SnapPointsAlignment="Center"
                            SnapPointsType="Mandatory"
                            Span="5"
                            VerticalItemSpacing="10" />
                    </CollectionView.ItemsLayout>
                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <StackLayout
                                VerticalOptions="Center"
                                HorizontalOptions="Center">

                                <Frame
                                    BackgroundColor="Orange"
                                    WidthRequest="{Binding Path=Height, Source={RelativeSource Mode=Self}}"
                                    HorizontalOptions="Center"
                                    Margin="0,3"
                                    Padding="0"
                                    CornerRadius="100">
                                    <Frame
                                        Margin="3"
                                        Padding="0"
                                        CornerRadius="100">
                                        <Button
                                            x:Name="asasd"
                                            Text="{Binding}"
                                            TextColor="Black"
                                            BackgroundColor="White"
                                            FontSize="20"
                                            VerticalOptions="Center"
                                            HorizontalOptions="Center"
                                            Margin="-5"
                                            Padding="5"
                                            Command="{Binding BindingContext.SelectNumberCommand, Source={Reference collectionView}}"
                                            CommandParameter="{Binding Text, Source={RelativeSource Mode=Self}}">
                                            <Button.Triggers>
                                                <DataTrigger
                                                    Binding="{Binding Path=BindingContext.SelectNumber, Source={Reference collectionView}, Converter={StaticResource NumbersButtonConverter}, ConverterParameter={Reference asasd}}"
                                                    TargetType="Button"
                                                    Value="true">
                                                    <Setter Property="BackgroundColor" Value="Orange" />
                                                </DataTrigger>
                                            </Button.Triggers>
                                        </Button>
                                    </Frame>
                                </Frame>
                            </StackLayout>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>
            </Grid>



        </Grid>
    </ContentPage.Content>
</ContentPage>